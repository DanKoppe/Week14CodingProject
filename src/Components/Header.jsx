import React from 'react';

const Header = () => { // function to return an H1 header at the top of the page
    return (
        <header>
            <h1>Movie Reviews</h1>
        </header>
    )
}

export default Header;